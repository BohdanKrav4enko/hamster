export type Ability = {
    id: number;
    title: string;
    price: number;
    income: number;
    owned: number;
};
export type LevelConfig = {
    level: number;
    coins: number;
    price: number;
};